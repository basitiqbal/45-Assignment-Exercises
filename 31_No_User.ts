let usernames: string[] = ['basit','moiz','farooq','admin']; // Empty array of usernames

if (usernames.length === 0) {
    console.log("We need to find some users!");
} else {
    usernames=[];
    console.log("remove name from user. "+ usernames.length);

}
